/**
 * SuiteCloud CLI project configuration.
 * Points the CLI at the SDF source folder so `suitecloud project:deploy`
 * and `suitecloud object:import` operate on ./src.
 */
module.exports = {
    defaultProjectFolder: 'src',
    commands: {}
};
